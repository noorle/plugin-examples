# Noorle plugin configuration
schema_version: "1.0"

metadata:
  name: python-sandbox
  description: "Python code execution sandbox - safely execute Python statements and evaluate expressions"
  author: "Noorle Team"
  tags:
    - python
    - sandbox
    - code-execution
    - eval

runtime: "v2"

# No special permissions needed - this is a pure computation sandbox
# No network access, no file system access, no environment variables
permissions: {}
