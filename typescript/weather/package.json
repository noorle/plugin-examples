{
  "name": "weather",
  "version": "0.1.0",
  "description": "Weather component for fetching current weather data (TypeScript)",
  "type": "module",
  "main": "app.ts",
  "scripts": {
    "build": "./build.sh",
    "typecheck": "tsc --noEmit",
    "compile": "tsc",
    "test": "node --test"
  },
  "dependencies": {
    "@bytecodealliance/componentize-js": "0.19.1",
    "@bytecodealliance/jco": "1.15.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^18.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}